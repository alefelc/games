{
  "name": "te-animas-web-deployment",
  "version": "5.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12"
  },
  "workspaces": [
    "packages/*",
    "games-main"
  ],
  "scripts": {
    "build:domain": "npm run build --workspace @te-animas/game-domain",
    "build:contracts": "npm run build:domain && npm run build --workspace @te-animas/contracts",
    "build:web": "npm run build:contracts && npm run build --workspace te-animas-app",
    "build": "npm run build:web"
  }
}
