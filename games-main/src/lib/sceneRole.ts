// Compatibility barrel. Scene-role normalization lives in the shared contract package.
export { normalizeSceneRole } from "@te-animas/contracts";
export type { SceneRole } from "@te-animas/contracts";
